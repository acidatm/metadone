export default class App{
	constructor(root,feed){
		this.connectNavigation()
	}
	connectNavigation(){
		let reels = document.getElementById("reels")
		let saved = document.getElementById("saved")
		let profile = document.getElementById("saved")
	}
}